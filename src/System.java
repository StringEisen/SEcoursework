public class System{
  public System{
      for(int t=0; t<4; t++){
         private Train train;
         setTrainId(t+1);
         setTrainAvailable(1);
         setTrainRun(0);
         setDriver(null);
      }
      for(int d=){
         private Driver d;
      }

      for(int r=){
         private Route r;
         for()
      }

      
  }


}